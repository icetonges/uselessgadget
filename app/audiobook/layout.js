export const metadata = {
  title: "The Level-Up Rule — Audiobook",
  description:
    "An interactive, read-along audiobook narrated by your browser's text-to-speech voices — follow the highlight, jump between chapters, and pick your narrator.",
};

export default function AudiobookLayout({ children }) {
  return children;
}
